{{> start }}

<title>Abilities | Pokémon: Tabletop Version</title>

<style>
  .abilityList_item {
    max-width: 30em;
    margin: 1em 1em 3em;
  }

  .ability_name {
    font: 700 1.3em/1.1 Helvetica, sans-serif;
  }

  p {
    margin: 0.6em;
  }
</style>

<dl class="abilityList">
  {{#abilities}}
    <div class="abilityList_item" id="{{slugify name}}">
      <dt class="ability_name">{{name}} <a href="#{{slugify name}}">🔗</a>

      {{#if bonusStats}}
        <dd class="ability_bonusStats">{{bonusStats}}
      {{/if}}

      <dd class="ability_desc">{{{description}}}
    </div>
  {{/abilities}}
</dl>
